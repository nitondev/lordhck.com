name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Build site
        run: |
          docker build -t lordhck \
            --build-arg COMMIT=$(git rev-parse --short HEAD) \
            --build-arg COMMIT_FULL=$(git rev-parse HEAD) \
            .
          docker create --name lordhck-out lordhck
          docker cp lordhck-out:/app/dist ./dist
          docker rm lordhck-out

      - name: Deploy to gh-pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
